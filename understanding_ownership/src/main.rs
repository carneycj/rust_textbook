fn main() {
    // s is not valid yet, since it has not been declared
    // This is a string literal
    let s = "hello"; // Now it's declared, so it is valid from this point forward
} // s is not valid, because it is out of scope now

// Pick up from the String type
